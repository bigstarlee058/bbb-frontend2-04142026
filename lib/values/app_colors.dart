import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0xff9A354E);
  static const secondColor = Color(0xFF87CEEB);
  static const blackColor = Colors.black;
  static const warmupColor =Color.fromRGBO(254, 233, 232, 1.0);
  static const backOffSetColor =Color.fromRGBO(232, 246, 255, 1.0);
  static const skipDayColor = Colors.blue;
  static List<Color> get primaryG => [primaryColor, primaryColor];
}
